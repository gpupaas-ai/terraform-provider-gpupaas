---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gpupaas_virtual_machine Resource - gpupaas"
subcategory: ""
description: |-
  GPU PaaS virtual machine (apiVersion: gpupaas.ai/v1alpha1, kind: VirtualMachine). Supports project- or workspace-scoped placement via metadata.workspace. Imperative lifecycle actions (start/stop/reboot) are driven through the desired_action attribute.
---

# gpupaas_virtual_machine (Resource)

GPU PaaS virtual machine (`apiVersion: gpupaas.ai/v1alpha1`, `kind: VirtualMachine`). Supports project- or workspace-scoped placement via metadata.workspace. Imperative lifecycle actions (start/stop/reboot) are driven through the `desired_action` attribute.

## Example Usage

```terraform
resource "gpupaas_virtual_machine" "trainer" {
  metadata = {
    name         = "trainer-01"
    project      = gpupaas_project.demo.metadata.name
    workspace    = gpupaas_workspace.team_a.metadata.name
    display_name = "Trainer 01"
    description  = "Primary training VM for team A"
  }

  spec = {
    virtual_machine = {
      name           = "A100-80G-x1"
      system_catalog = true
    }
    type             = "VM"
    cpu_count        = "16"
    memory           = "128Gi"
    security_group   = gpupaas_security_group.default.metadata.name
    ssh_key          = gpupaas_ssh_key.dev.metadata.name
    shared_storage   = gpupaas_storage.scratch.metadata.name
    assign_public_ip = true
    datacenter       = "us-west-2"
    image            = "ubuntu-22.04"
    boot_disk_size   = 200

    sharing = {
      share_mode = "SPECIFIC_WORKSPACES"
      workspaces = ["team-a", "team-b"]
    }
  }

  # Imperative lifecycle action. Changing this value (e.g. "none" -> "stop")
  # triggers a single backend action call during the next apply. Allowed
  # values: "none" (default, no-op), "start", "stop", "reboot".
  desired_action = "none"

  # Optional inputs forwarded with the action call (envs/variables).
  # action_inputs = {
  #   envs = {
  #     DEBUG = "1"
  #   }
  # }
}

output "trainer_public_ip" {
  value = try(gpupaas_virtual_machine.trainer.status.output.public_ip, null)
}

# Read-only audit metadata populated by the backend.
output "trainer_created_by" {
  value = try(gpupaas_virtual_machine.trainer.metadata.created_by.username, null)
}

# Import example (workspace-scoped):
# terraform import gpupaas_virtual_machine.trainer demo/team-a/trainer-01
# Or project-scoped:
# terraform import gpupaas_virtual_machine.trainer demo/trainer-01
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metadata` (Attributes) Standard metadata block (name, project, workspace, display_name, description, labels, annotations, created_by, modified_by). (see [below for nested schema](#nestedatt--metadata))
- `spec` (Attributes) (see [below for nested schema](#nestedatt--spec))

### Optional

- `action_inputs` (Attributes) Optional payload accompanying `desired_action`. Maps to `gpupaas.ActionOptions{Envs, Variables}`. Each value is sent as a single map entry in the SDK's list-of-maps wire format. (see [below for nested schema](#nestedatt--action_inputs))
- `desired_action` (String) Imperative lifecycle action to drive on this VM. One of `none`, `start`, `stop`, `reboot`. Changing this attribute (from its prior planned/state value) triggers the corresponding SDK call on the next apply. Setting it back to `none` (or omitting it) is a no-op — the provider never auto-re-triggers actions on refresh.

### Read-Only

- `api_version` (String)
- `id` (String) The ID of this resource.
- `kind` (String)
- `status` (Attributes) (see [below for nested schema](#nestedatt--status))

<a id="nestedatt--metadata"></a>
### Nested Schema for `metadata`

Required:

- `name` (String) Name of the resource. Combined with project/workspace to form the unique key.
- `project` (String) Project name that owns this resource. Maps to backend `metadata.project`.

Optional:

- `annotations` (Map of String) Key/value annotations propagated to the backend `metadata.annotations` field.
- `description` (String) Optional free-form description. Maps to backend `metadata.description`.
- `display_name` (String) Optional human-friendly name. Maps to backend `metadata.displayName`.
- `labels` (Map of String) Key/value labels propagated to the backend `metadata.labels` field.
- `workspace` (String) Workspace name that owns this resource. Maps to backend `metadata.workspace`.

Read-Only:

- `created_by` (Attributes) Backend-observed user that created the resource. Populated on reads; ignored on writes. (see [below for nested schema](#nestedatt--metadata--created_by))
- `modified_by` (Attributes) Backend-observed user that last modified the resource. Populated on reads; ignored on writes. (see [below for nested schema](#nestedatt--metadata--modified_by))

<a id="nestedatt--metadata--created_by"></a>
### Nested Schema for `metadata.created_by`

Read-Only:

- `is_sso_user` (Boolean)
- `options` (Attributes) (see [below for nested schema](#nestedatt--metadata--created_by--options))
- `username` (String)

<a id="nestedatt--metadata--created_by--options"></a>
### Nested Schema for `metadata.created_by.options`

Read-Only:

- `description` (String)
- `override` (Attributes) (see [below for nested schema](#nestedatt--metadata--created_by--options--override))
- `required` (Boolean)

<a id="nestedatt--metadata--created_by--options--override"></a>
### Nested Schema for `metadata.created_by.options.override`

Read-Only:

- `restricted_values` (List of String)
- `type` (String)




<a id="nestedatt--metadata--modified_by"></a>
### Nested Schema for `metadata.modified_by`

Read-Only:

- `is_sso_user` (Boolean)
- `options` (Attributes) (see [below for nested schema](#nestedatt--metadata--modified_by--options))
- `username` (String)

<a id="nestedatt--metadata--modified_by--options"></a>
### Nested Schema for `metadata.modified_by.options`

Read-Only:

- `description` (String)
- `override` (Attributes) (see [below for nested schema](#nestedatt--metadata--modified_by--options--override))
- `required` (Boolean)

<a id="nestedatt--metadata--modified_by--options--override"></a>
### Nested Schema for `metadata.modified_by.options.override`

Read-Only:

- `restricted_values` (List of String)
- `type` (String)





<a id="nestedatt--spec"></a>
### Nested Schema for `spec`

Required:

- `virtual_machine` (Attributes) Virtual machine catalog reference. (see [below for nested schema](#nestedatt--spec--virtual_machine))

Optional:

- `additional_block_size` (Number)
- `assign_public_ip` (Boolean)
- `block_storage_type` (String)
- `boot_disk_size` (Number)
- `cpu_count` (String)
- `create_additional_block` (Boolean)
- `datacenter` (String)
- `dns_servers` (List of String)
- `guest_password` (String, Sensitive)
- `image` (String)
- `memory` (String)
- `name` (String) Optional logical name; falls back to metadata.name.
- `security_group` (String)
- `shared_storage` (String)
- `sharing` (Attributes) Resource sharing across workspaces or projects. (see [below for nested schema](#nestedatt--spec--sharing))
- `ssh_key` (String)
- `subnet` (String)
- `timezone` (String)
- `type` (String)
- `user_data` (String)
- `vm_id` (String) Inventory device ID of the virtual machine. Usually observed from the backend; can be set by clients to pin a VM to a specific device. Maps to `spec.vmId` (wire field `vm_id`).
- `vpc` (String)

<a id="nestedatt--spec--virtual_machine"></a>
### Nested Schema for `spec.virtual_machine`

Required:

- `name` (String)

Optional:

- `system_catalog` (Boolean)


<a id="nestedatt--spec--sharing"></a>
### Nested Schema for `spec.sharing`

Required:

- `share_mode` (String) Sharing mode (e.g. `ALL_WORKSPACES`, `SPECIFIC_WORKSPACES`, `SPECIFIC_PROJECTS`).

Optional:

- `projects` (Attributes List) Project sharing entries when `share_mode = SPECIFIC_PROJECTS`. (see [below for nested schema](#nestedatt--spec--sharing--projects))
- `workspaces` (List of String) Workspaces shared with when `share_mode = SPECIFIC_WORKSPACES`.

<a id="nestedatt--spec--sharing--projects"></a>
### Nested Schema for `spec.sharing.projects`

Required:

- `name` (String)

Optional:

- `workspaces` (List of String)




<a id="nestedatt--action_inputs"></a>
### Nested Schema for `action_inputs`

Optional:

- `envs` (Map of String) Environment variables passed to the action.
- `variables` (Map of String) Action variables (action-specific key/value bag).


<a id="nestedatt--status"></a>
### Nested Schema for `status`

Read-Only:

- `action` (String)
- `last_connected_at` (String)
- `output` (Attributes) (see [below for nested schema](#nestedatt--status--output))
- `provisioned_at` (String)
- `reason` (String)
- `status` (String)

<a id="nestedatt--status--output"></a>
### Nested Schema for `status.output`

Read-Only:

- `disk_mount_path` (String)
- `host_name` (String)
- `os_name` (String)
- `private_ip` (String)
- `public_ip` (String)
- `server_host` (String)
- `user_name` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Virtual machines accept either a project-scoped or workspace-scoped import ID:
#   <project>/<name>
#   <project>/<workspace>/<name>
terraform import gpupaas_virtual_machine.trainer demo/team-a/trainer-01
```
